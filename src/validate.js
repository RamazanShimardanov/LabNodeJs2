function validSolve(a, b, c){
    if (typeof a !== 'number' || typeof b !== 'number' || typeof c !== 'number') {
      return false;
      }
      return true;
}
module.exports.validSolve = validSolve;
